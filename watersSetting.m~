function waterSetting(Or, volind, xs, ys, zs)

r = nthroot(3*volind/(4*pi), 3);

volTotal = xspace*yspace*zspace;

a = 0;
b = pi;

xd = floor(xs/r);
yd = floor(ys/r);
zd = floor(zs/r);

for i=1:xd
   rx = a + (b-a).*rand(1, 1);
   ry = a + (b-a).*rand(1, 1);
   rz = a + (b-a).*rand(1, 1);
   for j=1:yd
      for k=1:zd
         
               





